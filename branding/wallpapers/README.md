# Wallpapers

Put your real wallpaper assets here.

Expected layout:
- `default/`
- `purple/`
- `fox/`

The project no longer ships generated placeholder wallpapers in the scaffold.
Use your repository assets and then run:

```bash
python3 scripts/rebuild-wallpaper-index.py branding/wallpapers
```

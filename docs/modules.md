# Modules

Modules extend the system without bloating the core.

Each module should eventually provide:
- a manifest
- package requirements
- enable logic
- disable logic
- config templates when needed

The current repository only defines the first manifest layer.
